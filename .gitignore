node_modules/
dist/
*.log
.DS_Store
/out/
/tmp/

# CAD test models and generated meshes — kept local, not committed
*.step
*.stp
*.STEP
*.STP
*.stl
*.STL
*.3mf

# — except the small own-made fixtures the publish test gate needs (test/units.ts,
#   test/face-info.ts, test/attributes.ts, test/abort.ts run in CI via prepublishOnly)
!/cube.step
!/cylinder.step
!/sphere.step
!/everything.step
